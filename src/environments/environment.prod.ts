export const environment = {
  production: true,
  apiUrl: "https://backend01-miscanchas-production.up.railway.app/api" 
  // ⚠️ OJO: Agregué "/api" al final porque vi en tu código que tus rutas empiezan con /api (ej: /api/canchas).
  // Si tu código en los servicios ya agrega el "/api", bórralo de aquí.
};